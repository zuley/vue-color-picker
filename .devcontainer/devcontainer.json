// 配置详情参考 https://aka.ms/devcontainer.json
{
	// 名称
	"name": "Node.js & TypeScript",
	// 镜像
	"image": "mcr.microsoft.com/devcontainers/typescript-node:18",

	// 要添加到开发容器的功能。 更多查看： https://containers.dev/features.
	"features": {
		"ghcr.io/NicoVIII/devcontainer-features/pnpm:1": {}
	}

	// 本地端口映射
	// "forwardPorts": [],

	// 容器创建后使用 postCreateCommand 运行命令
	// "postCreateCommand": "yarn install",

	// 辅助工具配置
	// "customizations": {},

	// 取消下方注释，使用 root 用户连接. 更多信息: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
